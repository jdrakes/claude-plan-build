---
max_turns: 10
allowed_tools: [Skill]
---

If I have applied to a position it should not show up in needs you. In review is not waiting on them, only applied. We should have collapsible section. I think each view should be a separate tab. Consider using a Vue component library. The data pulls are slow.
